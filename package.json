{
  "name": "progressive-overload-tracker",
  "version": "1.0.0",
  "description": "Full-stack Progressive Overload Tracker",
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cp -r dist ../dist",
    "start": "cd backend && npm start",
    "postinstall": "cd backend && npm install"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
